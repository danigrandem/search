import React from 'react';
// Import the SearchScreen component
import SearchScreen from "./screens/SearchScreen";

const App: React.FC = () => {
  return (
      // Render the SearchScreen component
      <SearchScreen />
  );
};

export default App;
